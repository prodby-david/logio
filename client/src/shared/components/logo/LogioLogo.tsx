export default function LogioLogo({ className }: { className?: string }) {
  return (
    <span
      className={`text-2xl font-black text-accent-violet ${className}`}
    >
      Logio
    </span>
  );
}
